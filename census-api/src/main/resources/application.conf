akka {
  log-level = INFO
  http.server.preview.enable-http2 = on
}

hmda {
  census {
    grpc {
      host = "0.0.0.0"
      host = ${?GRPC_CENSUS_HOST}
      port = 60082
      port = ${?GRPC_CENSUS_PORT}
    }
  }
}
